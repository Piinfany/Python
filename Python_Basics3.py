#Write a script that calculates the area of a rectangle based on user input.
wide = int(input("Enter the width of the rectangle : "))
length = int(input("Enter the length of the rectangle : "))
print("Area of a rectangle : ", wide*length)